const MovieCard = ({ img, name, date }) => {
  return (
    <div {...rest}>
      <div>
        <img src={img} alt={`${name}-poster`} />
      </div>
      <div>
        <h3></h3>
        <div></div>
      </div>
    </div>
  )
}
